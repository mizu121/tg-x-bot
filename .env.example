BOT_TOKEN=
APIFY_TOKEN=

# Runtime safety limits
MAX_CONCURRENT_DOWNLOADS=1
MAX_UPLOAD_MB=49
SEND_AS_DOCUMENT_MB=45
MAX_REELS_PER_REQUEST=5
DOWNLOAD_DIR=downloads
LOG_LEVEL=INFO

# Default keeps downloads smaller and cheaper than "best" while still using mp4.
YTDLP_FORMAT=bv*[height<=720][ext=mp4]+ba[ext=m4a]/b[height<=720][ext=mp4]/best[height<=720]/best

# Optional: mount a cookie file as a secret and point yt-dlp at it.
# Do not commit cookie files.
# YTDLP_COOKIE_FILE=/run/secrets/ytdlp_cookies.txt
